README

To execute: run "python3 Main.py {interval time between scrapes in minutes}" from terminal (without quotations or curly braces)

To add a pinging location: add a line to locations.txt with the latitude, longitude, and custom name of the location
- NOTE: custom names MUST NOT HAVE SPACES. Use underscores for names with multiple words instead